'use strict';

/**
 * transaction-completion-atm-card service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::transaction-completion-atm-card.transaction-completion-atm-card');
